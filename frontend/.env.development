# Loaded only by `npm run dev` (mode=development).
#
# Point this at wherever the backend is running locally. In production this file
# is ignored and .env.production takes over with an empty (same-origin) base.
VITE_API_URL=http://localhost:5236
